# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code — local dev notes + tooling (not part of the app)
CLAUDE.md
.claude/

# Go-live dev notes + throwaway scripts (local only — not part of the app)
connection_plan.md
backend_infrastructure_audit.md
# `scripts/*` rather than `scripts/`: git cannot re-include a file whose parent
# directory is excluded, and the verify-* scripts below run in `npm test`, so
# they have to be tracked.
scripts/*
!scripts/verify-agent-readiness.mjs
!scripts/verify-routing.mjs
!scripts/send-test-email.mjs

# Raw club-logo drops (processed into public/logos/, raw files stay local)
logo-drop/

.vercel
.env*
email-preview.html

# Capacitor: the native project is generated, not authored (see .github/workflows/ios.yml)
ios/
android/
