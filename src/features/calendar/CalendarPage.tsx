import { PagePlaceholder } from '@/components/PagePlaceholder'

export function CalendarPage() {
  return (
    <PagePlaceholder
      eyebrow="Calendar"
      title="Month & week"
      description="Personal and university dates as toggleable layers — not separate tabs. Built in step 4."
    />
  )
}
