import { PagePlaceholder } from '@/components/PagePlaceholder'

export function TodayPage() {
  return (
    <PagePlaceholder
      eyebrow="Today"
      title="Your launch view"
      description="What's due, what's next, and your GPA at a glance — with a polished empty state. Built in step 2."
    />
  )
}
