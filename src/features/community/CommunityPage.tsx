import { PagePlaceholder } from '@/components/PagePlaceholder'

export function CommunityPage() {
  return (
    <PagePlaceholder
      eyebrow="Community"
      title="Partnered-org events"
      description="A deliberately light, read-only event feed — not a social network. Built in step 5."
    />
  )
}
