import { PagePlaceholder } from '@/components/PagePlaceholder'

export function LandingPage() {
  return (
    <PagePlaceholder
      eyebrow="ConcordiaTracker"
      title="Stop guessing what's due."
      description="The value-first landing page — hero syllabus-parse demo and pricing (semester pass $15) — is built last, in step 8."
    />
  )
}
