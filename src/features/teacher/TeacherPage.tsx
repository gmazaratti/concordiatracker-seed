import { PagePlaceholder } from '@/components/PagePlaceholder'

export function TeacherPage() {
  return (
    <PagePlaceholder
      eyebrow="Teacher portal"
      title="A distinct context"
      description="Search/create a class, upload a blueprint, post an announcement — just enough to show it's separate from the student app. Built in step 6."
    />
  )
}
