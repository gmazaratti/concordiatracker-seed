import { PagePlaceholder } from '@/components/PagePlaceholder'

export function SettingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Settings"
      title="Profile & billing"
      description="Clean profile, transparent billing (semester pass), usage stats, and the theme switcher. Reached via the avatar menu — never a sidebar tab. Built in step 7."
    />
  )
}
