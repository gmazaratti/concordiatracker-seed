import { PagePlaceholder } from '@/components/PagePlaceholder'

export function CoursesPage() {
  return (
    <PagePlaceholder
      eyebrow="Courses"
      title="Your courses"
      description="Editable grades, provenance badges, the grade-needed calculator (free) and GPA what-if (paid), plus the syllabus parse-reveal. Built in step 3."
    />
  )
}
